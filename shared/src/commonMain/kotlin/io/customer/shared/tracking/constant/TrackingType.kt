package io.customer.shared.tracking.constant

/**
 * This is subset of [ActivityType] and contains events that have similar attributes but are
 * different in types.
 */
enum class TrackingType {
    EVENT,
    PAGE,
    SCREEN,
}
